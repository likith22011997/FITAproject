import { RenderStyleDirective } from './render-style.directive';

describe('RenderStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderStyleDirective();
    expect(directive).toBeTruthy();
  });
});
